// alert('All set!');
/*
    Objectives = Capture the username and show a custom "Welcome" message.
    Define the vars with .js
    Input elements = Prompt method from window object
    Output elements = Write method from document object
*/
    // Vars definition =
    var jsName;

    // Data insertion =
    jsName = window.prompt('Insert your name = ','Use this box to write your name');

    // Data output =
    document.write('Hello, ' + jsName + '. Welcome aboard =) ');
    // This line 15 code shows a custom "Hi" message